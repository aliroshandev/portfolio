import { LoadingSkeletonDirective } from './loading-skeleton.directive';

describe('LoadingSkeletonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingSkeletonDirective();
    expect(directive).toBeTruthy();
  });
});
