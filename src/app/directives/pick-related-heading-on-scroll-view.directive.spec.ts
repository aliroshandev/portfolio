import { PickRelatedHeadingOnScrollViewDirective } from './pick-related-heading-on-scroll-view.directive';

describe('PickRelatedHeadingOnScrollViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PickRelatedHeadingOnScrollViewDirective();
    expect(directive).toBeTruthy();
  });
});
